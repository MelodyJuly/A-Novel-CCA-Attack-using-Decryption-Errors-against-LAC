The code for collecting the errors in LAC V1.

main.c: the main function for collecting errors, single-thread. If several instances are required, do not forget to have some time latency between different instances.

random.h: header file.

sbatch_lac.sh: sbatch example if a server is used.
